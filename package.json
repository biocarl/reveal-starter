{
  "name": "reveal-starter",
  "version": "0.0.1",
  "scripts": {
    "build": "reveal-md reveal-intro.md --static _site --static-dirs=images"
  },
  "dependencies": {
    "reveal-md": "^5.4.1"
  }
}
